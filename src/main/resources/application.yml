spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/adego?serverTimezone=UTC&characterEncoding=UTF-8
    username: adego
    password: adego1234

  main:
    allow-bean-definition-overriding: true

  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true

server:
  servlet:
    encoding:
      charset: UTF-8
      force: true
    context-path: /api
